@model MarketPlace.WebUI.Models.ApplicationUser

<div class="row">
    <div class="col-sm-6">
        <h3>Ваши аукционы</h3>
        <p>Создайте аукцион и разместите ваш лот</p>
        <p>@Html.ActionLink("Обзор ваших аукционов", "List", "Auction", new { user = Model.UserName }, null)</p>
    </div>
    @if (User.IsInRole("Admin"))
    {
        <div class="col-sm-6">
            <h3>Управление полномочиями</h3>
            <p>Настройка полномочий пользователей сайта </p>
            <p>@Html.ActionLink("Перейти", "List", "Account")</p>
        </div>
    }
</div>
<div class="row">
    <div class="col-sm-6">
        <h3>Редактировать страницу</h3>
        <p>Измените ваши данные в случае необходимости</p>
        <p>@Html.ActionLink("Редактировать", "Edit", "Account")</p>
    </div>
    @if (User.IsInRole("Admin"))
    {
        <div class="col-sm-6">
            <h3>Управление категориями</h3>
            <p>Создайте новые категории для улучшения навигации по аукционам</p>
            <p>@Html.ActionLink("Перейти к странице категорий", "All", "Category")</p>
        </div>
    }
</div>
<div class="row">
    <div class="col-sm-6">
        <h3>Отзывы</h3>
        <p>Отзывы от других пользователей о вашей деятельности на сайте</p>
        <p>@Html.ActionLink("Перейти к отзывам", "List", "Feedback", new { userName = Model.UserName }, null)</p>
    </div>
    @if (User.IsInRole("Admin"))
    {
        <div class="col-sm-6">
            <h3>Жалобы</h3>
            <p>Рассмотрение жалоб пользователей</p>
            <p>@Html.ActionLink("Перейти к жалобам", "List", "Complaint")</p>
        </div>
    }
</div>
<div class="row">
    
</div>
